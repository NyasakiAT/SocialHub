<Window x:Class="SocialBar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SocialBar"
        mc:Ignorable="d"
        Title="SocialBar"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        d:DesignWidth="900"
        d:DesignHeight="600" ResizeMode="NoResize">

    <WindowChrome.WindowChrome>
        <WindowChrome
            ResizeBorderThickness="5"
            GlassFrameThickness="0"
            CornerRadius="0">
        </WindowChrome>
    </WindowChrome.WindowChrome>
    <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="10" x:Name="NotificationPanel">
        <StackPanel.Resources>
            <Style x:Key="StackPanelPadding" TargetType="Control">
                <Setter Property="Margin" Value="5,0,0,0" />
            </Style>
            <Style BasedOn="{StaticResource StackPanelPadding}" TargetType="ContentControl" />
        </StackPanel.Resources>
    </StackPanel>
</Window>